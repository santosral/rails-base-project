<h1><%= current_user.username %>'s POSTS</h1>
<% @foods.each do |food| %>
  <table>
      <tr>
        <td><%= link_to food.food_name, food_path(food) %></td>
        <td><%= cl_image_tag(food.food_key, :width=>100, :height=>100)%></td>
      </tr>
    </table>
<% end %>