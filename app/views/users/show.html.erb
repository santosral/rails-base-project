<h1><%= @selected_user.username %>'s POSTS</h1>
<% @foods.each do |food| %>
  <table>
    <tr>
      <td><%= link_to food.food_name, user_food_path(@selected_user, food) %></td>
      <td><%= cl_image_tag(food.food_key, :width=>100, :height=>100)%></td>
    </tr>
  </table>
<% end %>